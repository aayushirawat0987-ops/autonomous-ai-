{
  "version": 2,
  "buildCommand": "npx prisma generate && npx tsc",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
